<mvc:View controllerName="listaordini.controller.articoli.Articolo"
  xmlns:mvc="sap.ui.core.mvc"
  xmlns="sap.m"
  xmlns:l="sap.ui.layout"
  xmlns:f="sap.f">
  <f:DynamicPage showFooter="true">
    <f:title>
      <f:DynamicPageTitle>
        <f:heading>
          <Title text="{Mode>/title}"/>
        </f:heading>
      </f:DynamicPageTitle>
    </f:title>
    <f:content>
      <Panel >
        <l:Grid>
          <VBox >
            <Label text="{i18n>labelCodArticolo}"
              design="Bold"
              required="true" />
            <Input value="{
                path: 'Articolo>/CodArticolo',
                type: 'sap.ui.model.type.Integer',
                constraints: { min: 0 }
              }"
              editable="{= !${Mode>/isEdit} }"
              type="Number"
              width="24rem"
              maxLength="3"/>
            <Label text="{i18n>labelNomeArticolo}"
              design="Bold"
              required="true"
              class="sapUiSmallMarginTop" />
            <Input value="{Articolo>/NomeArticolo}"
              maxLength="50"
              width="24rem"/>
            <Label text="{i18n>labelImporto}"
              design="Bold"
              required="true"
              class="sapUiSmallMarginTop" />
            <Input value="{
                path: 'Articolo>/Importo',
                type: 'sap.ui.model.type.Integer',
                constraints: { min: 0 }
              }"
              type="Number"
              width="24rem"
              maxLength="4"/>
            <Label text="{i18n>labelQuantitaDisp}"
              design="Bold"
              required="true"
              class="sapUiSmallMarginTop" />
            <StepInput value="{Articolo>/QuantitaDisp}"
              min="0"
              max="20"
              textAlign="Begin"
              step="1"
              width="24rem" />
          </VBox>
        </l:Grid>
      </Panel>
    </f:content>
    <f:footer>
      <OverflowToolbar>
        <Button text="{i18n>btnBack}"
          type="Back"
          press="onBack"/>
        <ToolbarSpacer />
        <Button text="{i18n>btnSave}"
          type="Emphasized"
          press="onSave"/>
      </OverflowToolbar>
    </f:footer>
  </f:DynamicPage>
</mvc:View>